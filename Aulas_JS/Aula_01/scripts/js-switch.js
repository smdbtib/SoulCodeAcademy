var x = 3;

switch (x) {
    case 0:
        console.log("0 "+x+" o valor digitado está no banco de dados!");
        break;
    case 1:
        console.log("0 "+x+" o valor digitado está no banco de dados!");
        break;
    case 2:
        console.log("0 "+x+" o valor digitado está no banco de dados!");
        break;
    default:
        console.log( x + " não está no banco de dados!")    
}